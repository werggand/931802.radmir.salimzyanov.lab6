@model lab6.Models.FolderModel

@{
    ViewData["Title"] = "Create Folder";
}

<h1>Create New Folder</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" autofocus />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input hidden asp-for="ParentFolderId" class="form-control" value="@ViewBag.ParentId" />
            </div>
            <div class="form-group">
                <input type="submit" value="Create Folder" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="OpenFolder" asp-route-id="@ViewBag.ParentId">Back</a>
</div>

